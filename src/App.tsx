import React from 'react';

export default function App() {
  return (
    <div style={{minHeight:'100vh',display:'grid',placeItems:'center',fontFamily:'system-ui, -apple-system, Segoe UI, Roboto, Arial'}}>
      <div style={{background:'white',padding:24,borderRadius:12,boxShadow:'0 6px 30px rgba(0,0,0,.07)'}}>
        <h1 style={{marginTop:0}}>Logiciel Loubet-Victor</h1>
        <p>Déploiement OK ✅ — on branchera Supabase et l’app complète juste après.</p>
      </div>
    </div>
  );
}
